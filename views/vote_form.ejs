<%
  const matchDate = voteLink?.match?.playedAt
    ? new Date(voteLink.match.playedAt).toLocaleDateString('pt-BR')
    : '';
%>

<section class="max-w-3xl mx-auto px-4 py-8 md:py-12">
  <div class="mb-6 text-center space-y-2">
    <p class="text-xs uppercase tracking-[0.3em] text-horriver-orange">Horriver Plate</p>
    <h1 class="text-2xl md:text-3xl font-semibold text-horriver-light">Votação da pelada</h1>
    <p class="text-sm text-horriver-gray">
      Link único para <span class="text-horriver-light font-medium"><%= voteLink.player?.name %></span>
      — válido uma única vez.
    </p>
    <% if (matchDate) { %>
      <p class="text-[13px] text-horriver-gray">Partida de <span class="text-horriver-orange font-medium"><%= matchDate %></span></p>
    <% } %>
  </div>

  <div class="card-highlight">
    <div class="card-inner p-6 md:p-8 space-y-6">
      <p class="text-sm text-horriver-gray">
        Escolha os destaques da noite. Se alguém não se enquadrar na posição, deixe como "Sem voto".
      </p>

      <form action="/votar/<%= voteLink.token %>" method="POST" class="space-y-5">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
          <div class="flex flex-col gap-2">
            <label class="text-sm font-semibold text-horriver-light">Melhor goleiro</label>
            <select
              name="bestGoalkeeperId"
              class="bg-black/50 border border-horriver-border rounded-lg px-3 py-2 text-sm text-horriver-light"
            >
              <option value="">Sem voto</option>
              <% options.goalkeepers.forEach(function(player) { %>
                <option value="<%= player.id %>"><%= player.name %> (<%= player.position %>)</option>
              <% }); %>
            </select>
          </div>

          <div class="flex flex-col gap-2">
            <label class="text-sm font-semibold text-horriver-light">Melhor zagueiro / defensor</label>
            <select
              name="bestDefenderId"
              class="bg-black/50 border border-horriver-border rounded-lg px-3 py-2 text-sm text-horriver-light"
            >
              <option value="">Sem voto</option>
              <% options.defenders.forEach(function(player) { %>
                <option value="<%= player.id %>"><%= player.name %> (<%= player.position %>)</option>
              <% }); %>
            </select>
          </div>

          <div class="flex flex-col gap-2">
            <label class="text-sm font-semibold text-horriver-light">Melhor meia</label>
            <select
              name="bestMidfielderId"
              class="bg-black/50 border border-horriver-border rounded-lg px-3 py-2 text-sm text-horriver-light"
            >
              <option value="">Sem voto</option>
              <% options.midfielders.forEach(function(player) { %>
                <option value="<%= player.id %>"><%= player.name %> (<%= player.position %>)</option>
              <% }); %>
            </select>
          </div>

          <div class="flex flex-col gap-2">
            <label class="text-sm font-semibold text-horriver-light">Melhor atacante</label>
            <select
              name="bestForwardId"
              class="bg-black/50 border border-horriver-border rounded-lg px-3 py-2 text-sm text-horriver-light"
            >
              <option value="">Sem voto</option>
              <% options.forwards.forEach(function(player) { %>
                <option value="<%= player.id %>"><%= player.name %> (<%= player.position %>)</option>
              <% }); %>
            </select>
          </div>
        </div>

        <div class="flex flex-col gap-2">
          <label class="text-sm font-semibold text-horriver-light">Craque geral da pelada</label>
          <select
            name="bestOverallId"
            class="bg-black/50 border border-horriver-border rounded-lg px-3 py-2 text-sm text-horriver-light"
          >
            <option value="">Sem voto</option>
            <% options.all.forEach(function(player) { %>
              <option value="<%= player.id %>"><%= player.name %> (<%= player.position %>)</option>
            <% }); %>
          </select>
        </div>

        <div class="flex flex-col gap-2 text-[12px] text-horriver-gray bg-white/5 border border-horriver-border rounded-xl px-4 py-3">
          <div class="flex items-center gap-2 text-horriver-orange font-semibold text-xs">
            <span class="h-2 w-2 rounded-full bg-horriver-orange"></span>
            Lembrete: o link expira após enviar os votos.
          </div>
          <p>Se fechar a página antes de enviar, basta reabrir o mesmo link.</p>
          <p>Use o botão abaixo somente quando tiver certeza das escolhas.</p>
        </div>

        <button
          type="submit"
          class="w-full md:w-auto px-6 py-3 rounded-full bg-horriver-orange text-horriver-bg font-semibold text-sm hover:bg-orange-500 transition shadow-card-soft"
        >
          Enviar votos
        </button>
      </form>
    </div>
  </div>
</section>
