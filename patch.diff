*** Begin Patch
*** Update File: views/rankings.ejs
@@
   </header>
+
+  <!-- Link rápido para Story Top 10 -->
+  <div class="bg-black/40 border border-horriver-border rounded-2xl px-4 py-3 flex flex-col md:flex-row md:items-end gap-3 text-[11px] md:text-xs">
+    <div class="flex-1 min-w-[200px]">
+      <label class="block text-horriver-gray mb-1">Story do ranking</label>
+      <select id="storyShareType" class="w-full bg-horriver-dark border border-horriver-border rounded-lg px-2 py-1.5 text-horriver-light">
+        <option value="gols">Top 10 - Artilheiros</option>
+        <option value="assistencias">Top 10 - Assistências</option>
+        <option value="ga">Top 10 - G+A</option>
+        <option value="notas">Top 10 - Notas médias</option>
+        <option value="presencas">Top 10 - Presenças</option>
+        <option value="fotos">Top 10 - Fotos</option>
+        <option value="ponderada">Top 10 - Média ponderada</option>
+      </select>
+    </div>
+    <div class="flex items-center gap-2">
+      <button id="storyShareBtn" class="px-4 py-2 rounded-full bg-horriver-orange text-black font-semibold hover:bg-orange-500 transition text-xs md:text-sm">
+        Abrir Story
+      </button>
+      <span class="text-[11px] text-horriver-gray">Abre em nova aba já com os filtros de ano/mês.</span>
+    </div>
+  </div>
@@
   </section>
+
+  <script>
+    (() => {
+      const btn = document.getElementById("storyShareBtn");
+      const sel = document.getElementById("storyShareType");
+      if (!btn || !sel) return;
+      btn.addEventListener("click", () => {
+        const type = sel.value || "gols";
+        const year = <%- JSON.stringify(selYear) %>;
+        const month = <%- JSON.stringify(selMonth) %>;
+        const params = new URLSearchParams();
+        if (year) params.set("year", year);
+        if (typeof month !== "undefined") params.set("month", month);
+        const url = `/share/ranking/${type}?${params.toString()}`;
+        window.open(url, "_blank");
+      });
+    })();
+  </script>
*** End Patch
